# Argon Fullstack Developer Skill Test Attendance App

## How to
- Run `cp .env.example .env` to copy env file
- Fill the `.env` file
- Make sure the api already up and running
- Run `npm start` to start the application